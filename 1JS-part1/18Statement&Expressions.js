//STATEMENT & EXPRESSIONS:

//Expression:
//An expression is a piece of code that produces a value.

3 + 4; //is an expression because this is gonna produce a value.
1991;
true && false && !false;

//Statement:
//The Statement is like a bigger piece of code that is executed and which does not produce a value on itsel.

if (23 > 10) {
  const str = `23 is bigger`;
}

// Expressions produces a values, and the statement are like full sentences that translate our actions.So the actions that we want the program to perform,
