//Activating Strict Mode:

//"use strict";   //<-- This is strict mode

//This staatement has to be the very first statement in the script.

//So if we have any code before this then strict mode will not be activated.

//Comments are allowed because JavaScript will just ignore them but no code.

//Strict mode makes it easier for us developers to avoid accidental errors.

//Strict mode is a special mode that we can activate in JavaScript, Which makes it easier for us to write a secure JavaScript code

//1st strict mode forbids us to do certin things and 2nd it creates visible errors and the developer console, where in other situations javascript qould just fail silently.

/////////////////////////////////////////

//Without --> "use strict";

// let hasDriversLicense = false;
// const passTest = true;

// if (passTest) hasDriverLicense = true;
// if (hasDriversLicense) console.log(`I can drive :D`);

/////////////////////////////////////////

// "use strict";  //<-- activate

// let hasDriversLicense = false;
// const passTest = true;

// if (passTest) hasDriverLicense = true;
// if (hasDriversLicense) console.log(`I can drive :D`);

/////////////////////////////////////////

"use strict";

// const interface = `Auto`;
const private = 123;
// const if = 45;

/////////////////////////////////////////
